object Css {
    // Keep class names in one place for reasonable type-safety.
    // These must match the CSS class names in frontend/styles.css
    const val appContainer: String = "appContainer"
    const val error: String = "error"
}
